import { TextChangeDirective } from './text-change.directive';

describe('TextChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new TextChangeDirective();
    expect(directive).toBeTruthy();
  });
});
